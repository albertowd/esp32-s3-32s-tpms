#ifndef RGB_LED_COLORS_H
#define RGB_LED_COLORS_H

#include "RGBLEDColor.h"

namespace RGBLED
{
    namespace Colors
    {
        const RGBLED::Color BLACK(0.0, 0.0, 0.0);
        const RGBLED::Color BLUE(0.0, 0.0, 1.0);
        const RGBLED::Color GREEN(0.0, 1.0, 0.0);
        const RGBLED::Color RED(1.0, 0.0, 0.0);
        const RGBLED::Color WHITE(1.0, 1.0, 1.0);
    };
};

#endif
